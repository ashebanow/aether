require File.expand_path(File.join(File.dirname(__FILE__), "..", "support", "paths"))

And /^And I have not created any characters$/ do
  pending
end

Then /^Then I should see an empty list of characters$/ do
  pending
end

Then /^Then I should see my new character in the list$/ do
  pending
end
